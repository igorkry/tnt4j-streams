<project name="tnt4j-streams-core" default="build" basedir=".">

	<echo message="Processing ${ant.project.name}${line.separator}" level="info" />
	
	<condition property="isWindows">
	    <os family="windows" />
	</condition>

	<condition property="isUnix">
	    <os family="unix" />
	</condition>

	<target name="build" depends="mvn_windows, mvn_unix"/>

	<target name="mvn_windows" if="isWindows">
	    <exec dir="." executable="cmd">
	        <arg line="/c mvn clean package javadoc:aggregate install" />
	    </exec>
	</target>

	<target name="mvn_unix" if="isUnix">
	    <exec dir="." executable="sh">
	        <arg line="-c 'mvn clean package javadoc:aggregate install'" />
	    </exec>
	</target>
	
</project>
